apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mlflow-dev-tls
  namespace: mlflow-dev
spec:
  secretName: mlflow-dev-tls
  issuerRef:
    kind: ClusterIssuer
    name: selfsigned-issuer
  dnsNames:
    - mlflow.local
