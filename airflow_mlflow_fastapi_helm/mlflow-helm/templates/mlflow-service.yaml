# mlflow-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: mlflow-service
  namespace: mlflow
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mlflow
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
