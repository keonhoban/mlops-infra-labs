# envs/prod/monitoring/fastapi-prod-sm.yaml
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: fastapi-prod-sm
  namespace: monitoring-prod
  labels:
    release: monitoring-prod
spec:
  namespaceSelector:
    matchNames: ["fastapi-prod"]
  selector:
    matchLabels:
      app: fastapi-prod            # ← 서비스 라벨과 동일
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scheme: http
